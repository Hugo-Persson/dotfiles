import { HyperKeySublayer, open } from "../utils";

export const codeSubLayer: HyperKeySublayer = {
	y: {
		to: [{ key_code: "7", modifiers: ["shift", "left_alt"] }],
	},
	u: {
		to: [{ key_code: "8", modifiers: ["shift", "right_alt"] }],
	},
	i: {
		to: [{ key_code: "9", modifiers: ["shift", "right_alt"] }],
	},
};
